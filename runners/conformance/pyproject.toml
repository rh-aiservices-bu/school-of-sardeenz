[project]
name = "sardeenz-runner-conformance"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = []   # fastapi + httpx come from the two shims installed editable alongside

[project.optional-dependencies]
test = ["pytest"]

[tool.pytest.ini_options]
testpaths = ["."]
