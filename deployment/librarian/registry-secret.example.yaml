# TEMPLATE — do not commit real registry credentials. Create this with
# `oc create secret docker-registry` as shown in the README. OpenShift uses it to push the built OCI
# image; Apptainer reads the same Docker config to pull that image and push the SIF through ORAS.
apiVersion: v1
kind: Secret
metadata:
  name: sardeenz-librarian-registry
type: kubernetes.io/dockerconfigjson
data:
  .dockerconfigjson: REPLACE_ME_WITH_BASE64_DOCKER_CONFIG_JSON
