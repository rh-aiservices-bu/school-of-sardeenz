apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sardeenz-postgres
spec:
  accessModes: ['ReadWriteOnce']
  resources:
    requests:
      storage: 10Gi
  # storageClassName: gp3-csi # set per cluster when the default is unsuitable
