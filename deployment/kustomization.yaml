apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: sardeenz

resources:
  - prereq
  - control-plane
  - proxy
  - dashboard
  - sif-runner
  - librarian

labels:
  - pairs:
      app.kubernetes.io/part-of: sardeenz
    includeSelectors: false
