{
  "name": "sardeenz",
  "version": "2.0.0-dev",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "workspaces": [
    "control-plane",
    "dashboard",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently --names \"proxy,cp,dashboard,bff\" --prefix-colors \"yellow,magenta,green,cyan\" \"./scripts/dev-proxy.sh\" \"npm run dev -w @sardeenz/control-plane\" \"npm run dev -w @sardeenz/dashboard\" \"npm run dev:server -w @sardeenz/dashboard\"",
    "dev:logged": "concurrently --names \"proxy,cp,dashboard,bff\" --prefix-colors \"yellow,magenta,green,cyan\" \"./scripts/dev-proxy.sh --logged\" \"npm run dev:logged -w @sardeenz/control-plane\" \"npm run dev:logged -w @sardeenz/dashboard\" \"npm run dev:server:logged -w @sardeenz/dashboard\"",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "typecheck": "tsc --build",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "codegen": "npm run codegen -w @sardeenz/types",
    "logs:proxy": "tail -f logs/proxy.log",
    "logs:cp": "tail -f logs/control-plane.log",
    "logs:dashboard": "tail -f logs/dashboard.log",
    "logs:bff": "tail -f logs/dashboard-server.log",
    "logs:all": "tail -f logs/*.log",
    "logs:clear": "rm -rf logs/*.log"
  },
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "@redocly/cli": "^1.34.0",
    "@vitest/coverage-v8": "^3.2.1",
    "concurrently": "^10.0.3",
    "eslint": "^9.17.0",
    "openapi-typescript": "^7.6.1",
    "prettier": "^3.4.2",
    "typescript": "^5.7.3",
    "typescript-eslint": "^8.22.0",
    "vitest": "^3.2.1"
  }
}
