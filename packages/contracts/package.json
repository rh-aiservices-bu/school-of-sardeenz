{
  "name": "@sardeenz/contracts",
  "private": true,
  "type": "module",
  "scripts": {
    "validate": "ls specs/*.yaml >/dev/null 2>&1 && redocly lint specs/*.yaml || echo 'No specs to validate yet'"
  }
}
