{
  "name": "@sardeenz/contracts",
  "private": true,
  "type": "module",
  "scripts": {
    "validate": "if ls specs/*.yaml >/dev/null 2>&1; then redocly lint specs/*.yaml; else echo 'No specs to validate yet'; fi"
  }
}
