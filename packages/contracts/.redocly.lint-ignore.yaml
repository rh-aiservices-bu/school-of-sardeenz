# Intentionally unused components — defined now for Phase 2 consumption.
# Remove this file (or individual entries) once the schemas are referenced.
specs/proxy-control-plane.yaml:
  no-unused-components:
    - '#/components/schemas/RoutingMapUpdate'
